# Copy this file to config/local.toml for machine-local provider overrides.
# Keep secrets in .env, not in TOML and never in git.
#
# Typical .env for Mimo/Xiaomi-compatible live smoke:
# RESEARCHSENSEI_ENABLE_API_LLM=1
# RESEARCHSENSEI_LLM_PROVIDER=mimo
# MIMO_API_KEY=...
# UNPAYWALL_EMAIL=you@example.com
# RESEARCHSENSEI_CONTACT_EMAIL=you@example.com
# SEMANTIC_SCHOLAR_API_KEY=...
# OPENAI_COMPATIBLE_API_KEY=...

active_provider = "deepseek"

[providers.deepseek]
kind = "openai_compatible"
base_url = "https://api.deepseek.com"
api_key_env = "DEEPSEEK_API_KEY"
model = "deepseek-chat"
timeout_seconds = 60

[providers.mimo]
kind = "openai_compatible"
base_url = "https://api.xiaomimimo.com/v1"
api_key_env = "MIMO_API_KEY"
model = "mimo-v2.5-pro"
auth_header = "api-key"
timeout_seconds = 60

[providers.openai_compatible]
kind = "openai_compatible"
base_url = "https://api.example.com/v1"
api_key_env = "OPENAI_COMPATIBLE_API_KEY"
model = "example-model"
timeout_seconds = 60

[providers.custom]
kind = "openai_compatible"
base_url = "https://your-openai-compatible-endpoint.example/v1"
api_key_env = "OPENAI_COMPATIBLE_API_KEY"
model = "your-model-name"
auth_header = "authorization"
timeout_seconds = 60

[app]
workspace_dir = "workspace"
default_learning_mode = "reproducible_2h"
max_upload_mb = 80
parser_backend = "docling"

[server]
host = "127.0.0.1"
port = 8765
reload = false

[search]
execution = "uvx"
command = "paper-search"
sources = ["arxiv", "openalex", "semantic_scholar", "crossref", "dblp", "unpaywall"]
max_results = 80
timeout_seconds = 90
min_citation_count = 0
